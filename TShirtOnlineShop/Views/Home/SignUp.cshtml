@model  TShirtOnlineShop.Models.CUSTOMER

@{
    Layout = null;
}


<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8">
    <title>Sign Up</title>

    <!--css files-->
    <link rel="stylesheet" href="~/Content/css/main.css">

    <!--Fonts-->
    <link href="https://fonts.googleapis.com/css?family=Source+Sans+Pro" rel="stylesheet">

    <!--Icons-->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">

    <style media="screen">
        body,
        html {
            padding: 0;
            margin: 0;
        }
    </style>
</head>
<body>

    <!--=================================Content==================================-->
    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()
        <div id="signin-container">
            <div id="signin">

            </div>

            <div id="input">
                <h1>Sign Up</h1>

                <form action="#">
                    <div id="container">
                        <div class="input">
                            <label for="name"><b>Fullname</b></label>
                            @Html.TextBoxFor(model => model.CustomerFullName, new { @type = "text", @name = "name", @placeholder = "Enter your full name" })
                            @Html.ValidationMessageFor(model => model.CustomerFullName)
                            @*<input type="text" name="name" placeholder="Enter your fullname" required>*@
                        </div>

                        <div class="input">
                            <label for="phone"><b>Phone number</b></label>
                            @Html.TextBoxFor(model => model.CustomerPhoneNumber, new { @type = "text", @name = "phone", @placeholder = "Enter your phone number" })
                            @Html.ValidationMessageFor(model => model.CustomerPhoneNumber)
                            @*<input type="text" name="phone" placeholder="Enter your phone number" required>*@
                        </div>

                        <div class="input">
                            <label for="username"><b>Email</b></label>
                            @Html.TextBoxFor(model => model.CustomerEmail, new { @type = "email", @name = "username", @placeholder = "Enter your email " })
                            @Html.ValidationMessageFor(model => model.CustomerEmail)
                            @*<input type="email" name="username" placeholder="Enter your email">*@
                        </div>

                        <div class="input">
                            <label for="pwd"><b>Password</b></label>
                            @Html.TextBoxFor(model => model.CustomerPassword, new { @type = "password", @name = "psw", @placeholder = "Password has at least 8 letters, includes digits and special letters" })
                            @Html.ValidationMessageFor(model => model.CustomerPassword)
                            @*<input type="password" name="psw" placeholder="Password has at least 8 letters, includes digits and special letters" required>*@
                        </div>

                        <div class="text-link">
                            <a>@Html.ActionLink("I already have an account", "SignIn", "Home")</a> <br>
                        </div>

                        <button type="submit">Sign Up</button>
                    </div>
                </form>
            </div>

        </div>
    }

</body>

</html>
﻿
